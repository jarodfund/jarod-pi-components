# 资源发布约定

## 原则

资源 release 与客户端版本分别递增。未变化的 ZIP 保持原 SHA-256；不重新打包只为更换下载域名。客户端内置的公钥是信任起点，不能从同一不受信任的下载响应中替换公钥后继续安装。

## 发布前

1. 固定上游提交和依赖版本，保留适用许可与版权声明；核对本地快照是否有公开分发授权。
2. 在隔离目录准备资源，排除密钥、个人会话、浏览器登录态、临时技能和私人文件。
3. 使用产品仓库的打包脚本生成 ZIP 与签名目录；检查平台、资源 ID、required 文件、大小和摘要。
4. 在 GitHub 创建 draft Release。上传四个平台目录、公钥及目录引用的全部 ZIP；资产不可遗漏。
5. 验证远程资产名称、大小和摘要后再公开。公开后通过普通无鉴权下载路径检查 HTTPS 重定向、Range/206、签名和实际 ZIP 内容。
6. 新客户端必须在隔离配置中验证资源安装、中断恢复和更新；不能仅用网页下载成功代替客户端验收。

## 命名与更新

- 资源标签：`components-<release>`，例如 `components-2`。
- 资源资产：`<sha256>.zip`，目录保持规范 `packages/<sha256>.zip`。
- 资源源地址：`https://github.com/jarodfund/jarod-pi-components/releases/latest/download/`。
- 客户端程序更新是独立仓库、独立签名目录，不能混入此资源 Release。
- 不删除旧 release、覆盖同名资产或改变已有标签内容；修正资源使用新 release。

发布者检查通过后显式将对应版本设为 latest。并发维护时先核对远程状态，不使用强制覆盖。

## 国内镜像

只配置维护者认可的 HTTPS 镜像。镜像和 GitHub 必须提供相同签名目录与 ZIP 摘要。客户端可按来源顺序回退，地理位置本身不保证速度；目前没有启用国内镜像。

## 未验收平台

Linux 与 macOS 在 Windows 构建机上只能检查归档、架构、权限及签名，不能宣称实机运行已验收。需要在各平台分别运行启动、资源安装、会话恢复和主要工具验证。未完成原生运行时的资源必须保留为待验收状态。
